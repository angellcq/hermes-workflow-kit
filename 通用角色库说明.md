# Hermes 通用角色库说明（16 角色 · 跨项目无特定语言）

> 版本: v4.0 融合版 ｜ 日期: 2026-09-08
> 部署目标: `.hermes/通用角色库/*.md`（用户级 Claude Code 角色库）
> 同步命令: `bash scripts/sync-roles-to-profiles.sh`

---

## 一、角色库总览

Hermes v4.0 融合版采用 **16 个跨项目通用角色**，覆盖从需求拆解到代码审查到部署运维的全链路。

```
┌──────────────────────────────────────────────────────────┐
│                    通用角色库（16 个）                      │
├────────────┬─────────────────────────────────────────────┤
│ 全栈类 (4) │ backend / frontend / fullstack / mobile    │
│ 专业类 (4) │ dba / devops / test / security-reviewer    │
│ 审查类 (3) │ code-reviewer / architect / planner        │
│ 管理类 (5) │ project-manager / build-resolver /          │
│            │ docs-writer / tdd-guide / loop-operator    │
└────────────┴─────────────────────────────────────────────┘
```

---

## 二、角色清单（16 个）

### 2.1 全栈类（4）

| # | 角色 | 模型 | 职责 |
|---|------|------|------|
| 1 | `backend-developer` | sonnet | 通用后端开发（Java/Go/Python/Node/Rust/Kotlin 等自适应） |
| 2 | `frontend-developer` | sonnet | 通用前端开发（Vue/React/Svelte/微信小程序等自适应） |
| 3 | `fullstack-developer` | sonnet | 全栈开发（前后端贯通，FastAPI/Nest/Next 等全栈框架） |
| 4 | `mobile-developer` | sonnet | 移动端开发（iOS Swift/Android Kotlin/Flutter/RN） |

### 2.2 专业类（4）

| # | 角色 | 模型 | 职责 |
|---|------|------|------|
| 5 | `dba` | sonnet | 数据库设计与优化（MySQL/PG/DM/Oracle/Mongo/Redis 等） |
| 6 | `devops` | sonnet | CI/CD + 容器化 + 部署（Docker/K8s/GitHub Actions/GitLab CI） |
| 7 | `test-engineer` | sonnet | 测试（单测/集成/E2E/性能，跨语言工具栈） |
| 8 | `security-reviewer` | sonnet | 安全审计（OWASP Top 10 + 依赖漏洞扫描） |

### 2.3 审查类（3）

| # | 角色 | 模型 | 职责 |
|---|------|------|------|
| 9 | `code-reviewer` | sonnet | 通用代码审查（多语言适配 + 架构 smell 检测） |
| 10 | `architect` | opus | 架构决策（系统设计 + ADR + 选型权衡） |
| 11 | `planner` | opus | 实施计划制定专家（探索 → 计划 → 任务分解） |

### 2.4 管理类（5）

| # | 角色 | 模型 | 职责 |
|---|------|------|------|
| 12 | `project-manager` | haiku | PM 拆解/跟踪/风险/汇报（高频低成本任务） |
| 13 | `build-error-resolver` | sonnet | 构建错误修复（跨语言编译器/linter/依赖错误） |
| 14 | `docs-writer` | sonnet | 文档生成（README/API 文档/ADR/变更日志） |
| 15 | `tdd-guide` | sonnet | TDD 强制方法论（RED → GREEN → REFACTOR） |
| 16 | `loop-operator` | sonnet | 自主 Agent 循环监控（长时间任务保活） |

---

## 三、跨语言适配规则

每个角色在启动时，**自动按目标项目语言加载对应规范**（详见 `Hermes模板库/编码规范_跨语言.md` 与 `skills/cross-language/SKILL.md`）。

```
角色启动流程：
1. 读取 .hermes/通用角色库/<role>.md 的 system prompt
2. 调用 skills/cross-language 检测目标语言
3. 加载对应语言规范子集（来自 编码规范_跨语言.md）
4. 合并到角色 prompt
5. 开始工作
```

**示例**：
- `backend-developer` 检测到 Java 项目 → 加载 §2.2 Java 规范（JUnit 5 / Bean Validation / Stream API）
- `backend-developer` 检测到 Go 项目 → 加载 §2.2 Go 规范（gofmt / errors.Is / context.Context）
- `frontend-developer` 检测到 Vue 3 项目 → 加载 §5.2 Vue 3 规范（Composition API / Pinia）
- `frontend-developer` 检测到 React 项目 → 加载 §5.2 React 规范（Hooks / React Query / MSW）

---

## 四、调用约定

### 4.1 通过 Agent 工具调用

```javascript
// Claude Code 对话中
Agent({
  subagent_type: "backend-developer",
  prompt: "实现用户登录 API（Java Spring Boot）",
  run_in_background: true
})
```

### 4.2 通过 agent-bridge.sh 调用

```bash
# Print 模式（单任务）
bash agent-bridge.sh claude backend-developer "实现用户登录 API"

# 后台模式
bash agent-bridge.sh claude-bg backend-developer "..." --task-id t-001

# 并行模式
bash agent-bridge.sh parallel --tasks tasks.yaml
```

### 4.3 通过 Pipeline 自动调用

```bash
python scripts/pipeline.py --project my-app --task "..." --lang python
# pipeline 内部自动调用 backend-developer / frontend-developer 等
```

---

## 五、模型选择策略

| 任务类型 | 推荐模型 | 对应角色 |
|---------|---------|---------|
| 日常编码/审查 | sonnet | backend/frontend/code-reviewer 等 |
| 架构决策/ADR | opus | architect / planner |
| 高频低成本任务 | haiku | project-manager |
| 复杂推理/选型 | opus | architect / planner / security-reviewer |

**规则**：默认 sonnet，架构与决策类任务用 opus，PM 类高频任务用 haiku 节省成本。

---

## 六、与其他技能的配合

| 场景 | 加载技能 |
|------|---------|
| 启动编码任务 | `cross-language` + 角色库 |
| 改码前波及面评估 | `codegraph-review` |
| 七阶段流程派发 | `project-workflow` |
| 看板任务生命周期 | `kanban-executor` |
| 端到端自动跑 | `autonomous-delivery` |
| 多 Agent 后台并行 | `agent-bridge` |

---

## 七、部署

```bash
# ═══ 1. 复制角色库到 Claude Code 用户目录 ═══
mkdir -p .hermes/通用角色库
cp 通用角色库/*.md .hermes/通用角色库/

# ═══ 2. 一键同步到 Hermes profiles ═══
bash scripts/sync-roles-to-profiles.sh

# ═══ 3. 验证 ═══
ls .hermes/通用角色库/*.md          # 应有 16 个
hermes profile list               # 应有 16 个 profile
bash scripts/agent-bridge.sh roles # 应列出 16 个
```

---

## 八、扩展自定义角色

如需新增角色（如 `embedded-developer` 嵌入式开发）：

1. 在 `通用角色库/` 下新增 `<role>.md`
2. 按现有角色的 frontmatter 格式编写（参考 `backend-developer.md`）
3. 在本说明的 §二 加入该角色
4. 同步脚本会自动识别并部署

**建议**：除非有明确差异化需求，优先复用现有 16 个角色（覆盖了 90% 场景）。

---

> 本说明为角色库的**唯一清单事实源**；变更时同步更新《Hermes 制度层》§八、`CHANGELOG.md`。
> 角色清单由 `scripts/agent-bridge.sh roles` 运行时扫描 `通用角色库/*.md` 动态生成，
> 不再维护脚本内的硬编码数组。